# Snort3 MQTT IPS - Python Dependencies
# ===========================================
#
# RECOMMENDED: Install via apt (Debian/Kali/Ubuntu 2024+)
# sudo apt install python3-pandas python3-numpy python3-openpyxl \
#                  python3-paho-mqtt python3-scapy python3-psutil \
#                  python3-netifaces python3-paramiko
#
# OR use pip (in venv if needed):
# pip install -r requirements.txt

# Core dependencies
pandas>=1.5.0
numpy>=1.23.0
openpyxl>=3.0.0
paho-mqtt>=1.6.0
scapy>=2.5.0
psutil>=5.9.0
netifaces>=0.11.0
paramiko>=3.0.0

# Optional: ML packages (only needed for AI features)
# tensorflow>=2.10.0
# keras>=2.10.0

# Built-in (no install needed):
# - sqlite3 (Python built-in)
# - pickle (Python built-in, protocol 5 in Python 3.8+)
